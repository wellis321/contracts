# ============================================
# Social Care Contracts Management - Environment Configuration
# ============================================
# 
# Copy this file to .env and update with your actual values
# DO NOT commit .env to version control (it's in .gitignore)
#
# Usage: cp .env.example .env
# Then edit .env with your production values

# ============================================
# Application Environment
# ============================================
# Set to 'production' or 'prod' for production deployment
# This automatically:
# - Disables error display
# - Enables secure cookies (requires HTTPS)
# - Enables error logging
APP_ENV=development

# Application Name (displayed in emails, headers, etc.)
APP_NAME=Social Care Contracts Management

# Application URL (must use HTTPS in production)
# Examples:
#   Development: http://localhost
#   Production: https://yourdomain.com
APP_URL=http://localhost

# ============================================
# Database Configuration
# ============================================
# Database host (include port if not default 3306)
# Examples:
#   localhost
#   localhost:8889 (MAMP default)
#   db.example.com
#   192.168.1.100:3306
DB_HOST=localhost

# Database name
DB_NAME=social_care_contracts

# Database username
DB_USER=root

# Database password (leave empty if no password)
DB_PASS=

# Database character set (usually utf8mb4)
DB_CHARSET=utf8mb4

# ============================================
# Email Configuration
# ============================================
# Email address for "From" field in system emails
# This should be a valid email address on your domain
MAIL_FROM=noreply@yourdomain.com

# Email address for "Reply-To" field in system emails
# Users will reply to this address
MAIL_REPLY_TO=support@yourdomain.com

# ============================================
# Production Notes
# ============================================
# 
# 1. SECURITY:
#    - Never commit .env to version control
#    - Use strong database passwords
#    - Ensure APP_URL uses HTTPS in production
#    - Set APP_ENV=production in production
#
# 2. DATABASE:
#    - Create a dedicated database user with minimal required permissions
#    - Use strong passwords (16+ characters, mixed case, numbers, symbols)
#    - Consider using SSL for database connections in production
#
# 3. EMAIL:
#    - For production, consider using SMTP (SendGrid, Mailgun, etc.)
#    - Update Email.php to use SMTP instead of PHP mail() function
#    - Ensure SPF/DKIM records are configured for your domain
#
# 4. PERFORMANCE:
#    - Enable PHP OPcache in production
#    - Configure proper error logging
#    - Set up database connection pooling if needed
#
# 5. MONITORING:
#    - Set up error logging and monitoring
#    - Configure cron jobs for tender monitoring (see docs/TENDER_MONITORING_SETUP.md)
#    - Set up database backups
